import { DefaultTheme } from 'styled-components';

export const theme: DefaultTheme = {
  colors: {
    primary: '#0070f3',
    secondary: '#ff0000',
    tertiary: '#000000',
    background: '#ffffff',
  },
} as const;
